# This file contains conda packages to be installed with pip line by line.
# Use hash char as line comment separator.
# It is recommended all python packages to be installed with pip; but keep at least one package below (pip)!
pip     # placeholder, at least one package should be listed here.